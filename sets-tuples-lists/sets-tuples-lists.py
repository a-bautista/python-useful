grades = [77, 80, 90, 100, 105, 107]

# Tuples are immutable objects and they do not change, they remain the same at all times.
tuples_grades = (77, 80, 90, 95, 100, 105, 107, 108, 109) 

# Sets are unique and unordered objects. Although we have two 100s, only one will be printed. 
set_grades = {77, 80, 90, 100, 100}

print(set_grades)